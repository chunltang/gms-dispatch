package com.baseboot.service.dispatch.vehicle.commandHandle;

import com.baseboot.entry.dispatch.monitor.vehicle.Monitor;

public interface MonitorAttrHandle {

    void receiveCommand(Monitor monitor);
}
